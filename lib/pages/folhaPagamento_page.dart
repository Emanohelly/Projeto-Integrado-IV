import 'package:flutter/material.dart';

class FolhapagamentoPage extends StatelessWidget {
  const FolhapagamentoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Folha de Pagamento'),
      ),
    );
  }
}
